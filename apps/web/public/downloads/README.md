# App Downloads

Place production APK files in this directory:

- `naploo-customer.apk`
- `naploo-partner.apk`

The website links to these files with same-origin `/downloads/...` URLs so APK downloads do not require a separate public IP or public port.
